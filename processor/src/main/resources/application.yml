server:
    port: 4080
management:
    port: 4081

cloud:
    aws:
        stack:
            auto: false
        region:
            auto: true
            static: cn-north-1

spring:
    profiles.active: dev
---
spring:
    profiles: dev


yunsoo:
    environment: dev
    debug: true
    processor:
        client:
            dataapi:
                baseurl: http://localhost:3080/

---
spring:
    profiles: test

yunsoo:
    environment: test
    debug: true
    processor:
        client:
            dataapi:
                baseurl: http://localhost:3080/
