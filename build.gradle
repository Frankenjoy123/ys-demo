subprojects {
  apply plugin: "java"
  apply plugin: 'eclipse'
  apply plugin: 'idea'

  group 'com.yunsoo'
  
  repositories {
    mavenLocal()
    mavenCentral()
  }

  dependencies {
     testCompile "junit:junit:4+"
  }
}

