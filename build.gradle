subprojects {
    apply plugin: "java"
    apply plugin: 'eclipse'
    apply plugin: 'idea'

    group 'com.yunsoo'

    repositories {
        mavenLocal()
        mavenCentral()
    }

}

task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}
